<html><head><meta http-equiv="content-type" content="text/html; charset=utf-8" /></head><body><h3>DynamicData and EntityDataSource Updated with Entity Framework 6 Support</h3><p>Microsoft has added Entity Framework 6 support for <a href="http://www.nuget.org/packages/Microsoft.AspNet.DynamicData.EFProvider">DynamicData</a> and <a href="http://www.nuget.org/packages/Microsoft.AspNet.EntityDataSource">EntityDataSource</a> controls in addition to updated templates which make use of Microsoft.AspNet.EntityDataSource. The DynamicData package works either with <a href="http://msdn.microsoft.com/en-us/data/jj193542.aspx">code first</a> or <a href="http://msdn.microsoft.com/en-us/data/jj205424.aspx">model first</a> created using Entity Framework 6, which also installs the required page, entity and field templates.</p>
<p>In order to use DynamicData control, you should create a new ASP.NET Dynamic Data entities web application and add the Microsoft.AspNet.DynamicData.EFProvider NuGet package which will ultimately add a reference to the DynamicData EFProvider binary and installs the required templates.</p>
<p>You should then create model using EF code first or designer and add the below code in <a href="http://msdn.microsoft.com/en-us/library/1xaas8a2(v=vs.71).aspx">Global.asax</a>.cs file, which will register your Entity Framework model.</p>
<p><code> DefaultModel.RegisterContext(<br /> new Microsoft.AspNet.DynamicData.ModelProviders.EFDataModelProvider(() =&gt; newNorthwindEntities1()),<br /> newContextConfiguration { ScaffoldAllTables = true }); </code></p>
<p>On the other hand, Microsoft.AspNet.EntityDataSource package will install the runtime binary and EntityFramework version 6 <a href="http://www.nuget.org/">NuGet</a> package. You should also add the following tag prefix in web.config file as mentioned below</p>
<p><code>&lt;pages&gt;<br /> &lt;controls&gt;<br /> &lt;addtagPrefix=&quot;ef&quot;assembly=&quot;Microsoft.AspNet.EntityDataSource&quot;<br /> namespace=&quot;Microsoft.AspNet.EntityDataSource&quot; /&gt;<br /> &lt;/controls&gt;<br /> &lt;/pages&gt; </code></p>
<p>Finally, you should create a new web form page and bind the EntityDataSource control to either GridView or FormView using the code below</p>
<p><code> &lt;ef:EntityDataSourceID=&quot;GridDataSource&quot;runat=&quot;server&quot;EnableDelete=&quot;true&quot;/&gt; </code></p>
<p>The installed templates replace the default EntityDataSource control with Microsoft.AspNet.EntityDataSource package. It also includes few bug fixes which resolve few issues associated with Many-Many field template. However, the templates included with Microsoft.AspNet.DynamicData.EFProvider are oriented for C# and Web Application projects and will not work with WebSites projects.</p><br><br><br><br><br><br></body></html>